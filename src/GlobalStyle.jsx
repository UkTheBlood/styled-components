import { createGlobalStyle } from "styled-components";

const GlobalStyle = createGlobalStyle`          // createGlobalStyle API 사용
    body {
        font-family: "Helvetica", "Arial", sans-serif;
        line-height: 1.5;
    }
`

export default GlobalStyle